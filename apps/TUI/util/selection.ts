/**
 * Text selection utilities for terminal UI.
 */

export function getSelectedText(): string | null {
  // Terminal text selection is handled by the terminal emulator itself.
  // This is a placeholder for future programmatic selection support.
  return null;
}
